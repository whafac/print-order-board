import { SpecsPageClient } from "@/components/SpecsPageClient";

export default function SpecsPage() {
  return (
    <div className="min-h-screen bg-slate-50">
      <SpecsPageClient />
    </div>
  );
}
