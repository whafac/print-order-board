import { VendorsPageClient } from "@/components/VendorsPageClient";

export default function VendorsPage() {
  return (
    <div className="min-h-screen bg-slate-50">
      <VendorsPageClient />
    </div>
  );
}
